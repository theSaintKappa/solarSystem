{
    "name": "solar_system",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "deploy": "vite build && netlify deploy",
        "deploy-prod": "vite build && netlify deploy --prod",
        "preview": "vite preview"
    },
    "dependencies": {
        "three": "^0.148.0",
        "vite": "^4.0.4"
    }
}
